import React from 'react';
import PropTypes from 'prop-types';

const Players = () => {
    return (
        <div>
            
        </div>
    );
};

Players.propTypes = {
    
};

export default Players;